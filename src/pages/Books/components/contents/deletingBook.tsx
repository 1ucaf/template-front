import { Box, CircularProgress } from "@mui/material";

export const DeletingBookContent = (
  <Box sx={{display: 'flex', p: 2, justifyContent: 'center', alignItems: 'center'}}>
    <CircularProgress />
  </Box>
)